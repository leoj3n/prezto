#
# Sets $ZDOTDIR to the DeLorean location. 
#
# This file is created as ~/.zshenv when the DeLorean hits 88 MPH, and
# should not be edited. Instead, edit .zshenv inside the DeLorean:
#
# __ZDOTDIR__/.zshenv
#
# Zsh's default behavior for user-level .zshenv does not source a
# secondary .zshenv inside the $ZDOTDIR, so the .zshenv inside the
# DeLorean has to be sourced manually (as is done below).
# 
# Authors:
#   Joel Kuzmarski <leoj3n@gmail.com>
#

export ZDOTDIR="__ZDOTDIR__"

source "${ZDOTDIR}/.zshenv"
